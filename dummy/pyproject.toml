[project]
name = "railway-ingestion-dummy"
version = "0.1.0"
requires-python = ">=3.14"

[dependency-groups]
dev = [
  "pytest>=8.0"
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"